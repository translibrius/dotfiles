# Windows overrides — link.ps1 puts this at %APPDATA%/alacritty/alacritty.toml
[general]
import = ["~/.config/alacritty/alacritty.toml"]

[terminal.shell]
program = "powershell.exe"
args = ["-NoLogo"]

[[keyboard.bindings]]
key = "C"
mods = "Control|Shift"
action = "Copy"

[[keyboard.bindings]]
key = "V"
mods = "Control|Shift"
action = "Paste"
